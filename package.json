{
	"name": "github-stats-generator",
	"version": "1.0.0",
	"description": "Get user info and display in a nice format. Focus and language display and easy configuration and extensibility.",
	"main": "src/main.js",
	"scripts": {
		"dev": "yarn tsc --watch",
		"start": "node dist/main.js",
		"build": "yarn tsc --build",
		"clean": "rm -rf ./dist"
	},
	"type": "module",
	"author": "Redstonerayy",
	"license": "GPL-3.0-or-later",
	"dependencies": {
		"@octokit/graphql": "^5.0.5",
		"@octokit/rest": "^18.12.0",
		"dotenv": "^16.0.3"
	},
	"devDependencies": {
		"@octokit/types": "^9.2.1",
		"@types/node": "^20.0.0",
		"typescript": "^5.0.4"
	}
}
